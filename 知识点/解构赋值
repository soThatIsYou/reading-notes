数组
  (1) var [a, b, c] = [1, 2, 3];
  (2) 如果解构不成功，变量的值为undefined
  (3) 如果等号的右边不是数组（或者严格地说，不是可遍历的结构），那么将会报错。
  (4) 解构赋值允许指定默认值。var[foo=true]=[];
      如果默认值是表达式那么表达式是惰性求值的
      默认值生效的条件是对象的属性严格等于undefined

对象
  (1)对象的解构赋值是以下代码的简写
      var{foo:foo,bar:bar}={foo:"aaa",bar:"bbb"};

      解构赋值的变量都会同时被声明
      var可以重新声明，let和const会报错
字符串
    类似数组，把字符串转换成了数组
数值和布尔类型
    解构的时候会先转为对象




用途
  1、取值方便
  2、为函数参数设置默认值
