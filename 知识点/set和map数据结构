
1、set结构不会添加重复的值
    (1)向set中插入值时，值不会发生转换 1 和 “1”是两个不同的值
    (2)===认为NaN不等于自身，set认为NaN等于自身,其他规则都和===完全相同
    (3)两个对象总是不精确相等的
    (4)set 可以用来对数组去重
2、set有两个属性
    （1）size
    （2）constructor
3、set的方法
    操作方法
      （1）add(valeu)，添加某个值，返回set本身
      （2）delete(valeu) 删除某个值，返回一个布尔值，表示是否删除成功
      （3）has(valeu) 返回一个布尔值，表示参数是否为set成员
      （4）clear 清除所有成员，没有返回值
